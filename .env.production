# 生产环境配置文件
# 请根据实际情况修改以下配置

# 应用配置
NODE_ENV=production
PORT=3000

# 数据库配置
# 请替换为您的云MongoDB连接字符串
MONGODB_URI=mongodb://root:bt@photo@39.108.139.62:27017/photo

# Redis配置（如果使用）
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT配置
# 请生成一个强密码作为JWT密钥
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# 阿里云OSS配置
# 请替换为您的阿里云OSS配置
ALI_OSS_ACCESS_KEY_ID=your-oss-access-key-id
ALI_OSS_ACCESS_KEY_SECRET=your-oss-access-key-secret
ALI_OSS_BUCKET=your-oss-bucket-name
ALI_OSS_REGION=oss-cn-hangzhou
ALI_OSS_ENDPOINT=https://oss-cn-hangzhou.aliyuncs.com

# 文件上传配置
UPLOAD_MAX_SIZE=10485760  # 10MB
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,image/webp

# 安全配置
CORS_ORIGIN=https://your-domain.com
RATE_LIMIT_WINDOW=15  # 分钟
RATE_LIMIT_MAX=100    # 每个窗口期最大请求数

# 日志配置
LOG_LEVEL=info
LOG_FILE=./logs/app.log

# 邮件配置（如果需要）
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-email-password
# SMTP_FROM=noreply@your-domain.com

# 监控配置（可选）
# SENTRY_DSN=your-sentry-dsn
# ANALYTICS_ID=your-analytics-id

# 缓存配置
CACHE_TTL=3600  # 缓存时间（秒）

# API配置
API_PREFIX=/api
API_VERSION=v1

# 数据库连接池配置
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=2
DB_CONNECTION_TIMEOUT=30000

# 会话配置
SESSION_SECRET=your-session-secret-change-this
SESSION_MAX_AGE=86400000  # 24小时（毫秒）

# 文件存储配置
STORAGE_TYPE=oss  # local 或 oss
LOCAL_UPLOAD_PATH=./uploads

# 图片处理配置
IMAGE_QUALITY=80
IMAGE_MAX_WIDTH=1920
IMAGE_MAX_HEIGHT=1080
THUMBNAIL_WIDTH=300
THUMBNAIL_HEIGHT=300

# 备份配置
BACKUP_ENABLED=true
BACKUP_SCHEDULE=0 2 * * *  # 每天凌晨2点
BACKUP_RETENTION_DAYS=30